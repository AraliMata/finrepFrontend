name: flutter_frontend_test
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <4.6.3"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.4
  toggle_bar: ^1.0.0
  dio: ^4.0.6
  dotted_border: ^2.0.0+2
  file_picker: ^4.5.1
  iconsax: ^0.0.8
  liquid_progress_indicator: ^0.4.0
  responsive_sizer: ^3.0.5+1
  test: ^1.19.5
  dropdown_button2: ^1.4.0
  shared_preferences: <=2.0.15
  # flutter_session: ^0.1.1
  google_fonts: ^3.0.1
  universal_html: ^2.0.8
  get: ^4.6.3
  pdf: ^3.8.1
  syncfusion_flutter_pdf: ^20.1.57-beta
  syncfusion_flutter_datagrid_export: ^20.1.57-beta
  screenshot: ^1.2.3
  auto_size_text: ^3.0.0
  bordered_text: ^2.0.0
  open_file: ^3.2.1

  #flutter_bloc: ^6.1.1 #EEEEE
  #meta: ^1.1.8 
  #equatable: ^1.2.0 
  #uuid: ^2.1.0
  #sqflite: ^1.3.0
  #path_provider: ^1.6.5 #EEEEEE

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
